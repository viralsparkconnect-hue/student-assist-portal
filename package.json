{
  "name": "academic-support-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:frontend": "cd app && npm run dev",
    "dev": "vercel dev",
    "build": "cd app && npm run build",
    "start": "cd app && npm run preview"
  },
  "dependencies": {
    "formidable": "^2.1.2"
  }
}